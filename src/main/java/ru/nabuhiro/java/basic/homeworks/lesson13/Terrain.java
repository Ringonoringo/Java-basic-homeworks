package ru.nabuhiro.java.basic.homeworks.lesson13;

public enum Terrain {
    BOG, PLAIN, DENSEFOREST;

    /*public static double trip(double distance, Terrain terrain) {
        switch (terrain) {
            case BOG:

                return Car.fuel;
            case DENSEFOREST:
                return x - y;
            case PLAIN:
                return x * y;
            default:
                throw new IllegalArgumentException("Unsupported operation: " + terrain);
        }
    }*/
}
